# Reference only — configure triggers in the UI (see .harness/README.md).
# Enterprise stub: PR → pipeline ci (quality only).
trigger:
  name: pr
  identifier: proeventos_ci_pr
  enabled: true
  description: "UI on pipeline ci: PR Created + Updated + Reopened (not Merged)"
  orgIdentifier: PLACEHOLDER_ORG
  projectIdentifier: PLACEHOLDER_PROJECT
  pipelineIdentifier: ci
  stagesToExecute: []
  source:
    type: Webhook
    spec:
      type: Harness
      spec:
        type: PullRequest
        spec:
          connectorRef: ""
          autoAbortPreviousExecutions: true
          repoName: Homelab/ProEventos
          actions:
            - Open
            - Reopen
            - Edit
            - Sync
          payloadConditions:
            - key: targetBranch
              operator: Equals
              value: main
          headerConditions: []
  inputYaml: |
    pipeline:
      identifier: ci
      properties:
        ci:
          codebase:
            build:
              type: PR
              spec:
                number: <+trigger.prNumber>
